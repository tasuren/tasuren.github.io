# tasuren.github.io
tasurenの最低限のコンテンツしかないポートフォリオです。  
ベースのメニュー等のレイアウトを変更した際にいちいち全てのファイルに変更を入れるのは面倒なので、[miko](https://github.com/tasuren/miko)テンプレートエンジンを使っています。  
(`compile.py`でレンダリングをして`docs`フォルダに出力するようになっている。)